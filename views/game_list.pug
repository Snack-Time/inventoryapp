extends layout 

block content 
    h1= title
    if game_list.length 
        ul
            each game in game_list 
                li
                    a(href=game.url) #{game.name}
                    p by #{game.developer.name}
    
    else
        p There are no games.